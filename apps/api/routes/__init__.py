"""API routes package.

Purpose: group API routers by bounded context.
Responsibilities: expose infrastructure and authentication route modules.
Dependencies: FastAPI route modules.
Extension Notes: add future routers as siblings, not in app bootstrap.
"""

