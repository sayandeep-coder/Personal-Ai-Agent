"""Application entrypoint package.

Purpose: group deployable process entrypoints.
Responsibilities: keep API, CLI, and worker bootstraps separate.
Dependencies: process-specific packages.
Extension Notes: add new entrypoints as sibling packages only.
"""

