"""Google OAuth authentication package.

Purpose: expose Google OAuth client contracts.
Responsibilities: authorization URLs, token exchange, profile, refresh, revoke.
Dependencies: HTTP transport and settings.
Extension Notes: add service-specific Google clients outside auth.
"""

