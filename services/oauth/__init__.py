"""OAuth services package.

Purpose: contain OAuth token persistence use cases.
Responsibilities: encrypted storage, retrieval, refresh updates.
Dependencies: token repository and encryption service.
Extension Notes: add key rotation and token audit when schema supports it.
"""

